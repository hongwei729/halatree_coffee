/// Keys for persisted credentials used by splash auto-login.
class AuthStorage {
  AuthStorage._();

  static const String emailKey = 'cached_user_email';
  static const String passwordKey = 'cached_user_password';
}

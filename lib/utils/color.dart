import 'package:flutter/material.dart';

// App theme: light brown and white
const Color colorPrimary = Color(0xFFC4A77D);       // Light brown (primary)
const Color colorPrimaryDark = Color(0xFFA68B5B);   // Darker brown (accent, buttons – good contrast with white text)
const Color colorBackground = Color(0xFFFFFBF7);    // Off-white / cream
const Color colorSurface = Color(0xFFFFFFFF);       // Pure white (cards, sections)
const Color colorOnPrimary = Color(0xFFFFFFFF);     // Text on primary/button
const Color colorOnBackground = Color(0xFF3E2723);  // Dark brown text
const Color colorOnSurface = Color(0xFF5D4037);     // Muted brown text
const Color colorOutline = Color(0xFFD7CCC8);       // Light brown border
